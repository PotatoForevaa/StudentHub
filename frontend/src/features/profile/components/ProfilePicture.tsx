import styled from "styled-components"

const Picture = styled.img`
        width: 100px;
        height: 100px;
    `;

export const ProfilePicture = (props: string) => {
    
    return <Picture src="props"></Picture>
}